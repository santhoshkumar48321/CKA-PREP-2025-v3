This scenario auto-initializes the lab environment on launch.

Use the terminal to solve the task shown after startup.

When finished, run:

```bash
bash ./validate.bash
```

To reset resources:

```bash
bash ./cleanup.bash
bash ./LabSetUp.bash
```
